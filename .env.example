# Production Postgres (for Vercel)
# Copy this to .env and fill in your values
DATABASE_URL="postgres://user:password@host:5432/database?sslmode=require"
POSTGRES_URL="postgres://user:password@host:5432/database?sslmode=require"
PRISMA_DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=your-api-key"

# Local Development (optional - defaults to SQLite)
# For local SQLite, use: DATABASE_URL="file:./dev.db"
# For local Postgres, use: DATABASE_URL="postgres://..."
